# TODO
- [ ] Introduce shared `ApplicationFormData` type in `types/index.ts`
- [ ] Update `app/apply/page.tsx` to use typed `ApplicationFormData` and typed `Partial<ApplicationFormData>` callbacks
- [ ] Update `components/apply/Step1.tsx` to remove `any` and use shared types
- [ ] Update `components/apply/Step2.tsx` to remove `any` and use shared types
- [ ] Update `components/apply/Step3.tsx` to remove `any` and use shared types
- [ ] Update `components/apply/Step4.tsx` to remove `any` and use shared types
- [ ] Run `npm run lint` and fix issues
- [ ] Run `npm run build` and fix issues

